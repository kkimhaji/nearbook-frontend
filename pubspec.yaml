# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter

  # 네트워크
  dio: ^5.7.0
  
  # 상태관리
  flutter_riverpod: ^2.6.1
  
  # 소켓
  socket_io_client: ^3.0.0
  
  # BLE
  flutter_blue_plus: ^1.35.3
  
  # 로컬 저장소 (토큰 저장)
  flutter_secure_storage: ^9.2.2
  
  # 라우팅
  go_router: ^14.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0